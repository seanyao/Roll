# loop module responsibility map

<!-- GENERATED by scripts/gen-module-maps.mjs; DO NOT EDIT. -->

This file is derived only from `@responsibility` declarations in each source file header.

| File | Responsibility |
| --- | --- |
| `activity-signal.ts` | Normalizes per-agent signals into the standard activity signal model. |
| `adversarial.ts` | Drives the bounded adversarial review round state machine. |
| `alert-loop.ts` | Decides alert file mechanics: append, dedup, consume and notify. |
| `bootstrap-evidence.ts` | Gates bootstrap on verified pending-delivery evidence manifests. |
| `branch-canary.ts` | Verdicts on branch and worktree leak counts. |
| `builder-finalization.ts` | Turns raw builder-exit facts into a finalization verdict. |
| `ci-loop.ts` | Decides CI monitoring, the pre-run CI gate and red heal-or-alert. |
| `correction-actuator.ts` | Maps correction signals to conservative or auto actuator actions. |
| `correction-safety.ts` | Applies circuit-breaker safety limits to repeated correction signals. |
| `cycle-activity.ts` | Analyzes cycle activity segments from the event stream. |
| `cycle-observer.ts` | Emits runner-observed cycle signal facts during the build phase. |
| `cycle-role-summary.ts` | Projects one cycle's event stream into a readable role chain. |
| `design-contract.ts` | Declares the designer contract a designed story must write before execution. |
| `evaluator-artifact.ts` | Declares the evaluator artifact contract for verified execution. |
| `execution-profile.ts` | Selects the cheapest sufficient execution profile from risk signals. |
| `handoff.ts` | Pure projection of the durable cycle-handoff/v1 event family. |
| `managed-workspace.ts` | Projects workspace lifecycle facts and conservatively releases workspaces. |
| `morning-report.ts` | Composes morning report rows from cycle events. |
| `north-star.ts` | Tracks daily north-star metrics against targets. |
| `orchestrator.ts` | Conducts the inner cycle: pick, route, run, deliver and reconcile in order. |
| `recovery.ts` | Decides crash recovery and startup preflight from observed debris. |
| `remote-branch-gc.ts` | Decides the narrow sweep of stranded loop/cycle remote branches. |
| `repair-loop.ts` | Runs the strictly bounded repair loop between evaluate and execute. |
| `review-resize.ts` | Downgrades scope on the reviewer's independent too-big signal. |
| `self-downgrade.ts` | Decides self-downgrade and chain-depth caps from scope signals. |
| `signals.ts` | Declares the single turning-point signal table for the whole loop. |
| `skill-dispatch.ts` | Owns the parent-only contract for parallel skill dispatch children. |
| `transcript.ts` | Reduces a cycle's raw event stream into layered timeline entries. |
| `watch-render.ts` | Renders watch output rows from parsed events. |
| `watch-status.ts` | Projects watch status summaries with durable cycle lookup fallback. |
