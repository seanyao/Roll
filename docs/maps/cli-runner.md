# cli-runner module responsibility map

<!-- GENERATED by scripts/gen-module-maps.mjs; DO NOT EDIT. -->

This file is derived only from `@responsibility` declarations in each source file header.

| File | Responsibility |
| --- | --- |
| `agent-liveness.ts` | Distinguishes a slow peer review from an externally blocked agent. |
| `agent-routing.ts` | Routes cycle commands to eligible agents from the agent registry. |
| `agent-spawn.ts` | Spawns real agent processes for the orchestrator's spawn_agent commands. |
| `attest-gate.ts` | Gates the cycle on the acceptance-report evidence chain. |
| `attest-remediation.ts` | Remediates ac-map omissions with a surgical second pass. |
| `binary-staleness.ts` | Warns when the roll binary is too old for the loop. |
| `capture-facts-handler.ts` | Handles capture-facts cycle commands and records capture facts. |
| `capture-peer-helpers.ts` | Runs capture helper peer processes for the capture lane. |
| `captured-evidence.ts` | Binds CAPTURED evidence into the harness-owned evidence set. |
| `contract-snapshot.ts` | Freezes the acceptance contract at cycle start and detects drift. |
| `correction-actuator.ts` | Applies correction actions from the correction circuit verdict. |
| `correction-circuit.ts` | Runs the correction circuit verdict over the policy and cycle state. |
| `cycle-role-artifact-writer.ts` | Writes CycleRoleSummary artifacts from the event stream at cycle end. |
| `done-guard.ts` | Blocks the Done transition until merge and acceptance evidence are confirmed. |
| `environment-cleanup.ts` | Cleans up the post-cycle environment. |
| `evaluation-tier-stage.ts` | Turns a card's risk tier into evaluation depth at the capture stage. |
| `execution-profile.ts` | Persists the execution profile for the runner. |
| `executor.ts` | Executes emitted cycle commands as the runner adapter glue. |
| `exemption-audit.ts` | Audits existing screenshot exemptions read-only. |
| `exemption-stats.ts` | Reports the screenshot_exempt rate as an observability signal. |
| `failure-attribution.ts` | Attributes cycle failures to their root classes from the event stream. |
| `in-repo-backlog.ts` | Writes the in-repo backlog for the child worktree. |
| `ingest-gate.ts` | Applies the shift-left ingest soft gate. |
| `local-publish.ts` | Publishes deliveries locally through the shared push-time evidence gate. |
| `main-checkout-guard.ts` | Protects the shared main checkout during a cycle. |
| `managed-primary-workspace.ts` | Transactionally allocates the managed primary workspace set. |
| `managed-workspace-allocator.ts` | Allocates managed workspaces through the common boundary. |
| `managed-workspace-guidance.ts` | Renders stable operator guidance for workspace lifecycle refusals. |
| `node-ports.ts` | Manages the node ports file for cross-process coordination. |
| `pairing-gate.ts` | Enforces the cross-agent pairing runtime gate. |
| `peer-gate.ts` | Triggers the peer review hard gate. |
| `pending-pr-create.ts` | Queues branches whose PR could not be opened due to transient faults. |
| `pending-publish.ts` | Tracks pending-publish deliveries. |
| `pick-ranking.ts` | Ranks the next eligible card picks deterministically. |
| `ports.ts` | Defines the runner port adapter types for injected dependencies. |
| `project-map.ts` | Maps project state for story routing decisions. |
| `publish-lifecycle.ts` | Walks the publish lifecycle state machine for remote delivery. |
| `recovery-candidates.ts` | Finds recovery candidates from the event stream. |
| `repair-briefing-handler.ts` | Handles the repair-round warm-start briefing. |
| `resume-truth.ts` | Restores resume truth for interrupted cycles. |
| `round-journal-emit.ts` | Records a role's turn into the per-card round journal. |
| `run-cycle.ts` | Drives the pure cycle state machine to a terminal state. |
| `run-records.ts` | Reads and appends run records. |
| `runner-policy.ts` | Loads the runner policy and resume scope. |
| `runner-time.ts` | Provides runner time and cycle timing helpers. |
| `sandbox-boundary.ts` | Runs child commands inside the sandbox boundary. |
| `scoped-route.ts` | Routes story.execute commands to the scoped Builder. |
| `screen-lock-events.ts` | Derives screen-lock events from the event stream. |
| `selfheal-budget.ts` | Enforces the per-story self-heal agent-rotation budget. |
| `selfheal-switch.ts` | Decides and actuates the zero-TCR agent switch. |
| `setup-handlers.ts` | Handles the setup cycle commands for workspaces and submodules. |
| `skill-body.ts` | Resolves and reads a skill's SKILL.md body with frontmatter stripped. |
| `skill-dispatch-workspace.ts` | Allocates the child workspace for parent-owned Skill dispatch. |
| `skip-cards.ts` | Maintains the runtime poison-pill skip-list. |
| `spawn-agent-handler.ts` | Spawns an agent with the extracted usage and cost tracking. |
| `spawn-observers.ts` | Spawns observer processes for the adversarial lane. |
| `spawn-role-handler.ts` | Executes the adversarial-pairing spawn_role command. |
| `spawn-watchdog.ts` | Watches spawned sub-agents and fails the role on hang. |
| `submodule-worktree.ts` | Creates submodule-aware worktrees for delivery. |
| `tcr-observation-emit.ts` | Emits TCR observation events from the roll test side. |
| `terminal-handlers.ts` | Handles terminal commands for the remote publish path. |
| `terminal-run-handler.ts` | Persists terminal runs and reconciles post-terminal truth. |
| `usage-recovery.ts` | Recovers session-file usage for interrupted cycles. |
| `warm-sessions.ts` | Maintains warm agent sessions for the loop. |
| `worktree-bootstrap.ts` | Bootstraps worktrees with submodules and hooks. |
