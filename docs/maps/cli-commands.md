# cli-commands module responsibility map

<!-- GENERATED by scripts/gen-module-maps.mjs; DO NOT EDIT. -->

This file is derived only from `@responsibility` declarations in each source file header.

| File | Responsibility |
| --- | --- |
| `agent-list.ts` | Runs the `roll agent list` subcommand, listing the known agent registry with installed and current markers. |
| `agent.ts` | Runs the `roll agent` subcommand for agent registry and scope-config management. |
| `alert.ts` | Runs the `roll loop alert` subcommand, adapting core alert-loop decisions to the CLI presentation. |
| `attest-audit.ts` | Runs the `roll attest audit` subcommand, auditing Done stories for dangling evidence references. |
| `attest.ts` | Runs the `roll attest <story-id>` subcommand, composing the acceptance evidence chain into one report. |
| `backlog-mgmt.ts` | Runs the `roll backlog` write and maintenance subcommands (block, defer, unblock, promote, lint). |
| `backlog-sync.ts` | Runs the `roll backlog sync` subcommand, importing GitHub issues into the backlog. |
| `backlog.ts` | Runs the `roll backlog` display subcommand, parsing and rendering the backlog grouped by type. |
| `browser.ts` | Runs the `roll browser` setup, doctor, and update subcommands for managed browser operations. |
| `capture.ts` | Runs the `roll capture` subcommand for capture-policy migration, evidence repair, and readiness status. |
| `cast.ts` | Runs the `cast` subcommand, rendering the role casting table in the terminal. |
| `ci.ts` | Runs the `ci` subcommand, reporting GitHub Actions run status for the current HEAD. |
| `config-get.ts` | Runs the `roll config` read surface, porting the help, --list, and key-read paths from the bash oracle. |
| `config.ts` | Runs the `roll config` command surface, adding the write surface and the lang sub-command. |
| `cycle-model-swap.ts` | Runs the `roll loop cycle model-swap` subcommand for pending model-swap candidates. |
| `cycle.ts` | Runs the `cycle <id>` subcommand, replaying one cycle in the terminal. |
| `cycles.ts` | Runs the `cycles` subcommand, rendering the cycle ledger as a first-class command. |
| `dashboard.ts` | Runs the `roll loop status` subcommand, rendering the loop health dashboard. |
| `delta.ts` | Runs the protocol-only `roll delta` CLI with prepare, validate, conclude, and status plumbing. |
| `design.ts` | Runs the `roll design` subcommand, launching the design skill conversation with a captured transcript. |
| `doc.ts` | Runs the `doc` subcommand, rendering Charter and language guide markdown as readable text. |
| `doctor.ts` | Runs the `roll doctor` subcommand, porting the four-section environment health checks. |
| `dream-run-once.ts` | Runs the `roll dream run-once` subcommand, driving the nightly code-health scan agent. |
| `dream.ts` | Runs the `roll dream` subcommand, framing the dream service run and its output. |
| `effort.ts` | Runs the `roll effort` subcommand, rendering the read-only isolation-effort view. |
| `gc.ts` | Runs the `gc` subcommand, aging out surplus attest run directories per the archive verdict. |
| `idea.ts` | Runs the `roll idea` subcommand, the single user-facing card-capture entry point. |
| `index-gen.ts` | Runs the `index` subcommand, regenerating the authoritative index and the features index page. |
| `index.ts` | Registers every ported CLI subcommand in the bridge command table. |
| `init.ts` | Runs the `roll init` subcommand, scaffolding a new project with AGENTS.md and the roll structure. |
| `lang.ts` | Runs the `roll lang` subcommand, reading and persisting the UI language setting. |
| `loop-adversarial.ts` | Runs the `roll loop adversarial` subcommand, aggregating adversarial-pairing cohort metrics. |
| `loop-cycle-gates.ts` | Runs the `roll loop` cycle-gate subcommands (notify, enforce-tcr, precheck-ci). |
| `loop-events.ts` | Runs the `roll loop events` subcommand, tailing the shared per-project event log. |
| `loop-exhaustion-split.ts` | Runs the `roll loop exhaustion-split` subcommand, auto-splitting exhausted cards into sub-stories. |
| `loop-fmt.ts` | Runs the `roll loop fmt` subcommand, formatting the agent observation stream for the watch window. |
| `loop-go.ts` | Runs the `roll loop go` subcommand, starting a loop cycle over the routed next card. |
| `loop-goal.ts` | Runs the `roll loop goal` subcommand, reviewing and updating the project goal. |
| `loop-log.ts` | Runs the `roll loop log` subcommand, reading per-cycle logs from the cycle-logs directory. |
| `loop-maint.ts` | Runs the `roll loop` maintenance subcommands (reset, mute, unmute, gc, test). |
| `loop-pardon-skip-list.ts` | Runs the `roll loop pardon-skip-list` transition alias delegating to doctor pardon. |
| `loop-reconcile-merge.ts` | Runs the async merge write-back for the `roll loop` reconcile path. |
| `loop-reconcile-pending.ts` | Runs the `roll loop reconcile-pending` subcommand, a bounded one-pass reconcile of pending-merge PRs. |
| `loop-reconcile.ts` | Runs the `roll loop reconcile` subcommand, the I/O adapter for reconcile-from-main. |
| `loop-recover.ts` | Runs the `roll loop recover` subcommand, the supervised recovery path out of a no-progress stop. |
| `loop-retired.ts` | Prints retirement redirect stubs for the retired v2 tmux-popup loop commands. |
| `loop-review-resize.ts` | Runs the `roll loop review-resize` subcommand, re-splitting stories flagged too large. |
| `loop-run-once.ts` | Runs the `roll loop run-once` subcommand, the TS-first single-cycle runner. |
| `loop-runner-readout.ts` | Runs the `roll loop runner-readout` subcommand, reading the runner version readout. |
| `loop-runs.ts` | Runs the `roll loop runs` subcommand, a thin reader over the project's runs.jsonl. |
| `loop-self-downgrade.ts` | Runs the `roll loop self-downgrade` subcommand, downgrading a story's delivery tier. |
| `loop-signals.ts` | Runs the `roll loop signals` subcommand, reading scored runs for rubric signals. |
| `loop-state.ts` | Runs the `roll loop pause|resume` subcommand, the PAUSE gate and the run-state read. |
| `loop-watch.ts` | Runs the `roll loop watch` subcommand, the read-only live view of the loop. |
| `ls.ts` | Runs the `ls` subcommand, printing the cross-project registry. |
| `next.ts` | Runs the `roll next` subcommand, continuing the init and onboard journey with one command. |
| `north.ts` | Runs the `roll north` subcommand, rendering the North Star report. |
| `offboard.ts` | Runs the `offboard` subcommand, porting the v2 offboard flow. |
| `pair.ts` | Runs the cross-agent pairing CLI surface (`pair`). |
| `peer.ts` | Runs the `peer` subcommand, driving an external peer review session. |
| `prices-cost.ts` | Re-exports the canonical cost and currency resolution from @roll/core. |
| `prices-refresh.ts` | Runs the `prices refresh` subcommand, refreshing the versioned price snapshots. |
| `prices.ts` | Runs the `prices` subcommand, rendering the versioned price table. |
| `pulse.ts` | Runs the `pulse` subcommand, rendering today's delivery pulse one-screen. |
| `release.ts` | Runs the `roll release` subcommand, owning the whole release transaction in order. |
| `setup-shared.ts` | Provides the shared install and sync primitives used by setup, init, and update. |
| `setup.ts` | Runs the `roll setup` subcommand, porting the v2 setup flow and its step record buffer. |
| `showcase.ts` | Runs the `showcase` subcommand, orchestrating the golden-path standard E2E in a sandbox. |
| `skill-dispatch.ts` | Runs the `roll skill-dispatch` subcommand, the explicit parent-owned Skill dispatch command. |
| `skills.ts` | Runs the `skills` subcommand, porting the skills catalog audit, sync, and generate surface. |
| `status.ts` | Runs the `roll status` subcommand, rendering the one-screen sync health dashboard. |
| `story-new.ts` | Runs the `roll story new` subcommand, the single channel for minting a card folder. |
| `story-validate.ts` | Runs the `roll story validate` subcommand, self-checking a story against the structural rules. |
| `supervisor-journal.ts` | Runs the `roll supervisor journal` subcommand, reading and appending supervisor journal events. |
| `supervisor.ts` | Runs the `roll supervisor` subcommand, the project-level observe-and-advise supervisor. |
| `test.ts` | Runs the `roll test` subcommand, porting the isolation dispatcher and the test runner. |
| `tool.ts` | Runs the `tool` subcommand, surfacing tool readiness and policy checks. |
| `truth.ts` | Runs the `roll truth query` subcommand, the CLI entry point for deterministic delivery-truth queries. |
| `tune.ts` | Runs the `tune` subcommand, the second-order control-loop read face. |
| `update.ts` | Runs the `roll update` subcommand, upgrading the global roll install and re-syncing. |
| `version.ts` | Runs the `version` subcommand, reporting the running Roll version from the install tree. |
| `worktree-audit.ts` | Runs the `roll worktree audit` subcommand, a read-only worktree lifecycle audit. |
| `worktree-cleanup.ts` | Runs the `roll worktree cleanup` subcommand, safe recovery from branch-canary worktree pressure. |
